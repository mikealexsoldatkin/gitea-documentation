---
date: "2023-03-04T19:00:00+00:00"
slug: "labels"
sidebar_position: 13
aliases:
  - /zh-tw/labels
---

# 標籤

您可以使用標籤來分類問題和拉取請求，並改善對它們的概覽。

## 建立標籤

對於儲存庫，可以透過轉到 `問題` 並點擊 `標籤` 來建立標籤。

對於組織，您可以定義組織範圍內的標籤，這些標籤將與所有組織儲存庫共享，包括已存在的儲存庫和新建立的儲存庫。組織範圍內的標籤可以在組織 `設定` 中建立。

標籤必須有一個必需的名稱、一個必需的顏色、一個可選的描述，並且必須是排他性的或非排他性的（請參見下文的 `範圍標籤`）。

建立儲存庫時，您可以使用 `問題標籤` 選項確保存在某些標籤。此選項列出了一些在您的實例中[全域設定的標籤集](../administration/customizing-gitea.md)。建立儲存庫時，將建立其包含的所有標籤。

## 範圍標籤

範圍標籤用於確保最多隻有一個具有相同範圍的標籤分配給問題或拉取請求。例如，如果標籤 `kind/bug` 和 `kind/enhancement` 設定了排他選項，則一個問題只能被分類為 bug 或 enhancement。

範圍標籤的名稱中必須包含 `/`（不能在名稱的任一端）。標籤的範圍基於**最後一個** `/` 確定，因此例如標籤 `scope/subscope/item` 的範圍是 `scope/subscope`。

## 按標籤篩選

問題和拉取請求列表可以按標籤篩選。選擇多個標籤顯示分配了所有選定標籤的問題和拉取請求。

按住 alt 點擊標籤，將從列表中排除具有所選標籤的問題和拉取請求。
